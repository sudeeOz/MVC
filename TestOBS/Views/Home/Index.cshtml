<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mini OBS - Ana Sayfa</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <style>
        :root {
            --primary-color: #667eea;
            --secondary-color: #764ba2;
            --accent-color: #f093fb;
            --text-dark: #2d3748;
            --text-light: #718096;
            --card-shadow: 0 15px 35px rgba(0,0,0,0.1);
            --hover-transform: translateY(-8px);
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            background: linear-gradient(135deg, var(--primary-color) 0%, var(--secondary-color) 100%);
            min-height: 100vh;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            display: flex;
            flex-direction: column;
            overflow-x: hidden;
        }

        .animated-bg {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -1;
        }

        .floating-element {
            position: absolute;
            opacity: 0.1;
            animation: float 8s ease-in-out infinite;
        }

        .floating-element:nth-child(1) {
            top: 10%;
            left: 5%;
            animation-delay: 0s;
        }

        .floating-element:nth-child(2) {
            top: 70%;
            right: 10%;
            animation-delay: 3s;
        }

        .floating-element:nth-child(3) {
            top: 40%;
            left: 80%;
            animation-delay: 6s;
        }

        .floating-element:nth-child(4) {
            top: 20%;
            right: 30%;
            animation-delay: 2s;
        }

        .floating-element:nth-child(5) {
            bottom: 10%;
            left: 20%;
            animation-delay: 4s;
        }

        /* Animasyonlar */
        @@keyframes float {
            0%, 100% { 
                transform: translateY(0px) rotate(0deg); 
                opacity: 0.1;
            }
            25% { 
                transform: translateY(-30px) rotate(90deg); 
                opacity: 0.2;
            }
            50% { 
                transform: translateY(-60px) rotate(180deg); 
                opacity: 0.15;
            }
            75% { 
                transform: translateY(-30px) rotate(270deg); 
                opacity: 0.2;
            }
        }

        @@keyframes borderGlow {
            0% { opacity: 0.1; }
            100% { opacity: 0.3; }
        }

        @@keyframes logoFloat {
            0%, 100% { 
                transform: translateY(0px) scale(1); 
            }
            50% { 
                transform: translateY(-10px) scale(1.05); 
            }
        }

        @@keyframes glowPulse {
            0% { transform: translate(-50%, -50%) scale(0.8); opacity: 0.5; }
            100% { transform: translate(-50%, -50%) scale(1.2); opacity: 0.8; }
        }

        @@keyframes titleGlow {
            0% { filter: drop-shadow(0 0 5px rgba(102, 126, 234, 0.3)); }
            100% { filter: drop-shadow(0 0 20px rgba(102, 126, 234, 0.6)); }
        }

        .container {
            flex: 1;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 20px;
            position: relative;
        }

        .main-container {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(15px);
            border-radius: 25px;
            box-shadow: var(--card-shadow);
            padding: 4rem 3rem;
            max-width: 800px;
            width: 100%;
            text-align: center;
            position: relative;
            overflow: hidden;
            border: 1px solid rgba(255, 255, 255, 0.2);
        }

        .main-container::before {
            content: '';
            position: absolute;
            top: -2px;
            left: -2px;
            right: -2px;
            bottom: -2px;
            background: linear-gradient(45deg, var(--primary-color), var(--accent-color), var(--secondary-color));
            border-radius: 25px;
            z-index: -1;
            opacity: 0.1;
            animation: borderGlow 3s ease-in-out infinite alternate;
        }

        .logo-section {
            margin-bottom: 3rem;
            position: relative;
            z-index: 2;
        }

        .logo-container {
            position: relative;
            display: inline-block;
            margin-bottom: 1.5rem;
        }

        .logo-icon {
            font-size: 5rem;
            color: var(--primary-color);
            display: inline-block;
            animation: logoFloat 3s ease-in-out infinite;
            text-shadow: 0 5px 15px rgba(102, 126, 234, 0.3);
        }

        .logo-glow {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            width: 120px;
            height: 120px;
            background: radial-gradient(circle, rgba(102, 126, 234, 0.3) 0%, transparent 70%);
            border-radius: 50%;
            animation: glowPulse 2s ease-in-out infinite alternate;
            z-index: -1;
        }

        .main-title {
            font-size: 3.5rem;
            font-weight: 800;
            margin-bottom: 0.5rem;
            background: linear-gradient(135deg, var(--primary-color), var(--secondary-color), var(--accent-color));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            text-shadow: 0 5px 15px rgba(0,0,0,0.1);
            animation: titleGlow 4s ease-in-out infinite alternate;
        }

        .subtitle {
            color: var(--text-light);
            font-size: 1.3rem;
            margin-bottom: 1rem;
            font-weight: 500;
        }

        .description {
            color: var(--text-light);
            font-size: 1.1rem;
            margin-bottom: 3rem;
            line-height: 1.6;
            max-width: 600px;
            margin-left: auto;
            margin-right: auto;
        }

        .login-section {
            position: relative;
            z-index: 2;
        }

        .login-btn {
            background: linear-gradient(135deg, var(--primary-color), var(--secondary-color));
            color: white;
            border: none;
            padding: 1rem 3rem;
            border-radius: 50px;
            font-size: 1.2rem;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
            text-decoration: none;
            display: inline-flex;
            align-items: center;
            gap: 0.8rem;
            box-shadow: 0 8px 25px rgba(102, 126, 234, 0.3);
            position: relative;
            overflow: hidden;
        }

        .login-btn::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255,255,255,0.3), transparent);
            transition: left 0.6s;
        }

        .login-btn:hover::before {
            left: 100%;
        }

        .login-btn:hover {
            transform: var(--hover-transform) scale(1.05);
            box-shadow: 0 15px 40px rgba(102, 126, 234, 0.4);
        }

        .login-btn:active {
            transform: translateY(-2px) scale(1.02);
        }

        .login-icon {
            font-size: 1.3rem;
            animation: iconBounce 2s ease-in-out infinite;
        }

        @@keyframes iconBounce {
            0%, 100% { transform: translateX(0); }
            50% { transform: translateX(5px); }
        }

        .features {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 1.5rem;
            margin-top: 3rem;
            opacity: 0.9;
        }

        .feature-item {
            background: rgba(255, 255, 255, 0.6);
            padding: 1.5rem;
            border-radius: 15px;
            text-align: center;
            transition: all 0.3s ease;
            border: 1px solid rgba(255, 255, 255, 0.3);
        }

        .feature-item:hover {
            background: rgba(255, 255, 255, 0.8);
            transform: translateY(-5px);
            box-shadow: 0 10px 25px rgba(0,0,0,0.1);
        }

        .feature-icon {
            font-size: 2rem;
            color: var(--primary-color);
            margin-bottom: 0.5rem;
        }

        .feature-text {
            color: var(--text-dark);
            font-size: 0.9rem;
            font-weight: 500;
        }

        .footer {
            text-align: center;
            padding: 20px;
            color: rgba(255, 255, 255, 0.8);
            font-size: 0.9rem;
        }

        .footer a {
            color: white;
            text-decoration: none;
            margin: 0 10px;
            transition: opacity 0.3s ease;
        }

        .footer a:hover {
            opacity: 0.7;
        }

        @@media (max-width: 768px) {
            .main-container {
                padding: 2.5rem 1.5rem;
                margin: 10px;
            }

            .main-title {
                font-size: 2.5rem;
            }

            .logo-icon {
                font-size: 3.5rem;
            }

            .features {
                grid-template-columns: 1fr;
                gap: 1rem;
            }

            .login-btn {
                padding: 0.8rem 2rem;
                font-size: 1.1rem;
            }
        }

        @@media (max-width: 480px) {
            .main-title {
                font-size: 2rem;
            }
            
            .description {
                font-size: 1rem;
            }
            
            .login-btn {
                width: 100%;
                max-width: 280px;
            }
        }
    </style>
</head>
<body>
    <div class="animated-bg">
        <div class="floating-element">
            <i class="fas fa-graduation-cap" style="font-size: 4rem;"></i>
        </div>
        <div class="floating-element">
            <i class="fas fa-book-open" style="font-size: 3rem;"></i>
        </div>
        <div class="floating-element">
            <i class="fas fa-user-graduate" style="font-size: 3.5rem;"></i>
        </div>
        <div class="floating-element">
            <i class="fas fa-chalkboard-teacher" style="font-size: 3rem;"></i>
        </div>
        <div class="floating-element">
            <i class="fas fa-university" style="font-size: 4rem;"></i>
        </div>
    </div>

    <div class="container">
        <div class="main-container">
            <div class="logo-section">
                <div class="logo-container">
                    <div class="logo-glow"></div>
                    <div class="logo-icon">
                        <i class="fas fa-university"></i>
                    </div>
                </div>
                <h1 class="main-title">Mini OBS</h1>
                <p class="subtitle">Öğrenci Bilgi Sistemi</p>
                <p class="description">
                    Modern ve kullanıcı dostu arayüzü ile öğrenci, öğretmen ve yönetici işlemlerinizi 
                    kolayca gerçekleştirin. Güvenli giriş yapın ve eğitim yönetim sisteminize erişim sağlayın.
                </p>
            </div>

          <a class="login-btn" asp-controller="Account" asp-action="Login">
    <i class="fas fa-sign-in-alt login-icon"></i>
    Giriş Yap
</a>



          
        </div>
    </div>

    <footer class="footer>my-2">
        <p>
            <a href="#" onclick="showPrivacy()">Gizlilik</a> |
            <a href="#" onclick="showTerms()">Kullanım Koşulları</a> |
            <a href="#" onclick="showContact()">İletişim</a>
        </p>
        <p>&copy; 2024 Mini OBS. Tüm hakları saklıdır.</p>
    </footer>

    <script>
        function goToLogin() {
            // Bu fonksiyon login sayfasına yönlendirecek
            // ASP.NET MVC için:
            window.location.href = '/Home/Account';
            
            // Veya doğrudan login sayfanızın URL'ini kullanabilirsiniz
            // window.location.href = 'login.html';
        }

        function showPrivacy() {
            alert('Gizlilik politikası sayfası yakında eklenecek.');
        }

        function showTerms() {
            alert('Kullanım koşulları sayfası yakında eklenecek.');
        }

        function showContact() {
            alert('İletişim: info@miniobs.edu.tr');
        }

        // Sayfa yüklendiğinde animasyon efektleri
        document.addEventListener('DOMContentLoaded', function() {
            const mainContainer = document.querySelector('.main-container');
            mainContainer.style.opacity = '0';
            mainContainer.style.transform = 'translateY(50px)';
            
            setTimeout(() => {
                mainContainer.style.transition = 'all 1s cubic-bezier(0.175, 0.885, 0.32, 1.275)';
                mainContainer.style.opacity = '1';
                mainContainer.style.transform = 'translateY(0)';
            }, 100);
        });
    </script>
</body>
</html>